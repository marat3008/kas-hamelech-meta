require('dotenv').config();const express=require('express');const cors=require('cors');const {listConnectedAccounts}=require('./integrations/composio');const app=express();app.use(cors());app.use(express.json());app.get('/health',(req,res)=>res.json({status:'ok',business:'Kas Hamelech'}));app.get('/webhooks/meta',(req,res)=>{if(req.query['hub.verify_token']===process.env.META_VERIFY_TOKEN)return res.send(req.query['hub.challenge']);res.sendStatus(403);});app.post('/webhooks/meta',(req,res)=>res.sendStatus(200));app.get('/composio/status',async(req,res)=>{try{const data=await listConnectedAccounts();res.json({status:'ok',connectedAccounts:data});}catch(err){res.status(err.response?.status||500).json({status:'error',message:err.response?.data||err.message});}});app.get('/',(req,res)=>res.json({message:'Kas Hamelech Meta System',status:'running'}));const PORT=process.env.PORT||3000;app.listen(PORT,()=>console.log('Server running on port '+PORT));
