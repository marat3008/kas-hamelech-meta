# 📕 ספר ההפעלה הממומן — גרישה כ-Media Buyer | כס המלך עיצובים

**עבור:** גרישה — קניין מדיה (Media Buyer) ומנהל קמפיינים ממומנים
**מאת:** מרט אלטמן — כס המלך עיצובים
**קובץ אב:** `grisha-master-knowledge.md` (קרא אותו קודם)

> זהו ספר ההפעלה המלא לפעילות הממומנת. קרא אותו לפני נגיעה בכל קמפיין. בכל ספק — חזור לקו האדום.

---

## 🛑 הקו האדום — כלל־על אחד שגובר על הכול

> # ⛔ אסור להוציא כסף ללא אישור **בכתב** ממרט אלטמן

זה לא כלל בין כללים — זו המסגרת שכל השאר חי בתוכה. אתה מנוע לחלוטין, ללא יוצא מן הכלל, מ:

- ❌ העברת קמפיין / ad-set / מודעה ממצב `PAUSED` ל-`ACTIVE`
- ❌ הגדרה, העלאה או שינוי של תקציב חי (Live budget) — יומי או כולל
- ❌ **בוסט (Boost) לפוסט אורגני** — זו הוצאה כספית לכל דבר
- ❌ כל התחייבות כספית: שיתוף פעולה בתשלום, קידום ממומן, ספונסרשיפ

**מה זה "בכתב":** הודעה חד־משמעית ומתועדת (וואטסאפ/אימייל) שבה מרט מאשר במפורש את **הפעולה + התקציב + הקמפיין הספציפי**. בעל־פה, רמז, "נראה טוב", אמוג'י של אגודל, או שתיקה — **אינם אישור**.

**מה כן מותר לך לעשות בעצמאות מלאה:**
- לתכנן קמפיינים, מבנה חשבון, קהלים ותקציבים מוצעים
- **ליצור** קמפיינים/ad-sets/מודעות במצב `PAUSED` (ברירת מחדל בקוד: `status = 'PAUSED'`)
- להכין קריאייטיב, קופי, קהלים, מעקב
- לנתח ביצועים, להמליץ על אופטימיזציה, להכין דוחות
- להגיש למרט המלצה מוכנה־להפעלה עם כפתור אחד שנשאר לו ללחוץ

**רגע הוצאת הכסף שייך אך ורק למרט.** תפקידך הוא להביא כל דבר עד מילימטר לפני הרגע הזה.

> 💡 בקוד: `createCampaign()` ב-`ads/meta-ads.js` נפתח תמיד עם `status: 'PAUSED'`. אל תשנה זאת, ואל תקרא ל-`updateCampaignStatus(id, 'ACTIVE')` ללא אישור בכתב.

---

## 🧱 שכבת עובדות המוצר — כלל אי-ההמצאה

לצד הקו האדום הכספי יש קו אדום שני: **אסור להמציא עובדות מוצר.** כל פרט המסומן ב-❓ אסור למלא ללא אישור מרט:

- ❓ טווחי מחיר
- ❓ זמן ייצור / זמן אספקה
- ❓ חומרים ובדים, ספקים
- ❓ אחריות, מדיניות משלוח והחזרות
- ❓ כתובת סטודיו מדויקת, טענת "תוצרת ישראל" בניסוח מחייב

בקופי ובפוסטים: מלא `[סוגריים]` **רק** בעובדה מאושרת. אם אין — השאר placeholder והסלם למרט. ליד שנסגר על סמך הבטחה שקרית גרוע פי כמה מליד שלא נסגר.

---

## 1. תפקיד ה-Media Buyer — מה גרישה עושה כל יום

| תדירות | פעולה |
|---|---|
| יומי | בדיקת delivery, מודעות שנדחו, זמן תגובה ללידים (≤ שעה), אנומליות בהוצאה |
| יומי | מענה/ניתוב לידים חדשים (`operations/lead-flow.md`) |
| 2-3×/שבוע | בדיקת frequency, CPL/CPM, רענון קריאייטיב עייף |
| שבועי | `getDashboardSummary()` → סקירת מנצחים/מפסידים, המלצות תקציב למרט, תיעוד ב-`learning-loop/` |
| חודשי | סקירת אסטרטגיה, התאמה לעונתיות (חגים), תכנון קמפיין הבא |

**עקרון־על:** לרכז, לא לפזר. עדיף מעט ad-sets שיוצאים משלב הלמידה (≈50 אירועים/שבוע) על פני הרבה ad-sets רעבים.

---

## 2. מבנה החשבון (Account Structure)

```
Campaign (מטרה עסקית אחת)
 └─ Ad Set (קהל + תקציב + מיקומים + תזמון)
     └─ Ad (קריאייטיב + קופי + CTA)
```

**מטרות (Objectives) בשימוש:**
- `MESSAGES` — Click-to-WhatsApp (המשפך הראשי; ברירת המחדל ב-`createCampaign`)
- Lead Ads (טופס לידים מקומי של Meta → webhook `leadgen`)
- מאוחר יותר, אחרי איסוף דאטה: `CONVERSIONS` מבוסס CAPI

**כלל תקצוב:** התחל ב-CBO (Campaign Budget Optimization) רק כשיש ≥3 ad-sets יציבים; אחרת תקציב ברמת ה-ad-set. תקציב ❓ — לקביעה עם מרט.

---

## 3. המשפך (Funnel)

```
מודעה (FB/IG/TikTok)
   │
   ├─ Click-to-WhatsApp  → שיחה ב-WhatsApp → מענה אוטומטי (meta-system) → העברה לנציג → סגירה ייעוצית
   └─ Lead Ad (טופס)     → webhook leadgen → שמירת ליד + התראת אימייל → הודעת WhatsApp פותחת → סגירה ייעוצית
```

**המכירה ייעוצית ונסגרת בשיחה** — לא בעגלת קניות. לכן היעד של המודעה הוא **שיחה איכותית**, לא "מכירה" באתר.

**שלוש שכבות משפך (חלוקת תקציב מנחה):**
- **~30% Awareness** — וידאו קר, קהלים רחבים/lookalike קרים. מטרה: להכיר את המותג והאומנות.
- **~60% Consideration + Conversion** — קהלים חמים יותר, Click-to-WhatsApp ו-Lead Ads. הליבה שמייצרת שיחות.
- **~10% Retargeting** — מי שצפה בווידאו/נכנס לפרופיל/פתח שיחה ולא סגר.

---

## 4. קהלים (Audiences)

- **קר:** אינטרסים (עיצוב פנים, רהיטים, אירוח, חתונות/אירועים משפחתיים) + טירגוט גאוגרפי לישראל, עם התאמת מגזר (ראה `brand/audience-shabbat-observant.md`).
- **Lookalike:** מבוסס על רשימת לקוחות/פותחי שיחות (כשנצטבר מספיק דאטה).
- **חם / Retargeting:** צופי וידאו (25%/50%/75%), מבקרי פרופיל, פותחי שיחת WhatsApp שלא נסגרו, לידים פתוחים.

**רגישות מגזרית:** בקהל חרדי — שמרנות בדימויים (צניעות חובה), פחות TikTok, יותר WhatsApp ו-word-of-mouth. בספק — לכיוון השמרני.

---

## 5. תזמון — שבת וחגים (מחייב)

**אין הפעלה, מודעות פעילות, בוסטים או הודעות אוטומטיות בשבת ובחג.**

- לעצור מודעות **לפני הדלקת נרות** (שישי) ולחדש **במוצ"ש**.
- בחגים — עצירה מלאה לאורך כל החג.
- **תמיד לאמת זמני כניסה/יציאה ב-Hebcal לעיר הרלוונטית — לא מהזיכרון.**
- זה גם כבוד וגם ביצועים: הקהל לא מחובר, וכסף שרץ בשבת מתבזבז.

מכיוון שהעברת קמפיין ל-`ACTIVE`/`PAUSED` דורשת נגיעה בכסף — תכנן מראש עם מרט לוח עצירה/הפעלה שבועי, או קבל ממנו אישור בכתב לאוטומציה של העצירות. פירוט: `strategy/annual-calendar-jewish.md`.

---

## 6. קריאייטיב וקופי — רק מאושר

השתמש אך ורק בזוויות ובמזהי הקופי מ-`creative/`:

- **זוויות:** A1 כבוד שבת · A2 אירוח · A3 מוכנות לחג (❓ תלוי זמן ייצור) · A4 ריפוד/לפני-אחרי · A5 התאמה אישית · A6 קסם האומנות.
- **מזהי קופי ממומן:** Click-to-WhatsApp `CW-1..3` · Lead Ads `LA-1..2`.
- כל וריאציה ב-A/B מקבלת מזהה ומתועדת ב-`learning-loop/experiments.md`.

---

## 7. אופטימיזציה שבועית — צ'ק-ליסט

הרץ `getDashboardSummary()` (ב-`ads/meta-ads.js`; מחזיר spend, leads, CPL, קמפיינים פעילים) ואז:

1. **סקל מנצחים** ~20% (לא יותר — קפיצה גדולה מאפסת את הלמידה).
2. **עצור מפסידים** — ad-sets מתחת לסף CPL/סגירה שהוגדר.
3. **רענן קריאייטיב עייף** — Frequency > 3–4 או ירידת CTR.
4. **בדוק זמן תגובה** ללידים — יעד ≤ שעה. זמן תגובה איטי הורג ROAS יותר מ-CPM גבוה.
5. **הצלב CPL עם אחוז סגירה** — ליד זול שלא נסגר = מלכודת. תמיד יחד.
6. **תעד** כל שינוי ולקח ב-`learning-loop/LEARNING_LOG.md`.

> ⚠️ כל שינוי תקציב חי או הפעלה = דורש אישור בכתב. שינויי קריאייטיב/קהל/מבנה בתוך קמפיין קיים — בעצמאותך, כל עוד לא נוגעים בסכום ההוצאה או במצב ההפעלה.

---

## 8. מדידה נכונה

- **כוכב צפון:** ROAS ו-CAC.
- **מדד מוביל יומיומי:** עלות לשיחת WhatsApp / CPL — **תמיד עם אחוז הסגירה**.
- **שים לב לפער המדידה:** ב-CAPI (`capi/conversions.js`) הפונקציות `trackContact` ו-`trackLead` מחוברות, אבל `trackSchedule` ו-`trackPurchase` **קיימות אך אינן נקראות בשום מקום**. עד שיחוברו, Meta מאפטמת לשיחות/לידים — **לא להכנסה**. אל תסיק ROAS ישיר מהפיקסל עד לחיבור. פירוט: `operations/kpis.md`, `troubleshooting/`.

---

## 9. הסלמה למרט — מתי לעצור ולשאול

הסלם מיד (ואל תמשיך לבד) כאשר:
- נדרשת הוצאת כסף / הפעלת תקציב / בוסט → **תמיד**.
- חשבון פרסום מושבת או מוגבל (Account Quality).
- דרוש פרט מוצר מסומן ❓ כדי להתקדם.
- מודעה נדחתה שוב אחרי ערעור, או חשד להפרת מדיניות מהותית.
- אנומליה חדה בהוצאה/ביצועים שאתה לא מבין.

**עד לאישור:** ממשיכים לתכנן, להכין קריאייטיב, לבנות קמפיינים ב-`PAUSED`, ולייצר תוכן אורגני. הגלגלים מסתובבים — רק ברז הכסף סגור עד שמרט פותח אותו בכתב.

---

*חלק ממאגר הידע החי של כס המלך עיצובים. עדכן ותעד ב-`learning-loop/`.*
